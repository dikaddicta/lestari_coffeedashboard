# Premium Patch v22

- Removed hero micro-label line (`BREW • LOG • QA • STOCK • RECIPE`).
- Replaced hero coffee emoji with a local 2D latte art PNG badge (`assets/latte-art-icon.png`).
- Adjusted hero heading sizing so the `S` in `OS` no longer clips.
- Refined table headers to use a seamless, elegant premium gradient across each table header row.
- Tuned table scrollbar styling to better match the coffee palette.
