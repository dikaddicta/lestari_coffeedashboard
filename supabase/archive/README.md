# Supabase SQL

- `schema.sql` dipakai untuk setup database baru.
- `migration_*.sql` dipakai untuk database lama yang sudah pernah memakai versi sebelumnya.
- Jalankan migration sesuai urutan di README utama.
- Jangan menaruh service role key di frontend atau repo publik.
